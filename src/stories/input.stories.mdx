import {useState} from 'react';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action, actions } from "@storybook/addon-actions";

import { Input } from '../components/ui/Input/';
let test = "test";

<Meta title="FusedComponents / Input" component={Input} />

# Input

## An input component
 

## Not Active
<Preview>
    <Story name="Default Input" parameters={{ decorators: [withKnobs] }}>
    <Input
     ariaLabel="Text Input" 
      {...actions({onChange:"toggle action change"})}
    />
    </Story>
</Preview>

## Disabled 
<Preview>
    <Story name="Disabled Input" parameters={{ decorators: [withKnobs] }}>
    <Input
    ariaLabel="Disabled Input" 
        disabled={boolean("Disabled", true, 'disabled')}
    />
    </Story>
</Preview>

## Properties

<Props of={Input} />
