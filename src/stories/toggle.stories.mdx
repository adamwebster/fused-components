import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action } from "@storybook/addon-actions";

import Toggle from '../components/ui/Toggle/';

<Meta title="FusedComponents|Toggle" component={Toggle} />

# Animated Button

## An button that has different states and animation

Can set different properties to display different states for the button. 

<Preview>
    <Story name="Default" parameters={{ decorators: [withKnobs] }}>
    <Toggle
        onClick={action("toggle action click")}
        active={boolean("Active", false)}
    />
    </Story>
</Preview>

<Preview>
    <Story name="Active" parameters={{ decorators: [withKnobs] }}>
    <Toggle 
    active={boolean("Active", true, "Active")}
    />
    </Story>
</Preview>


## Properties

<Props of={Toggle} />
