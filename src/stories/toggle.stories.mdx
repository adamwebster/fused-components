import {useState} from 'react';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action, actions } from "@storybook/addon-actions";

import { Toggle } from '../components/ui/Toggle/';
let test = "test";

<Meta title="FusedComponents|Toggle" component={Toggle} />

# Toggle

## An toggle that has an active and not active state
 
To change the state of the toggle update the active property to set to true or false.
## Not Active
<Preview>
    <Story name="Default" parameters={{ decorators: [withKnobs] }}>
    <Toggle
      {...actions({onClick:"toggle action click", onMouseOver: "toogle action mouseover"})}
        active={boolean("Active", false)}
    />
    </Story>
</Preview>

## Active
<Preview>
    <Story name="Active" parameters={{ decorators: [withKnobs] }}>
    <Toggle 
    active={boolean("Active", true, "Active")}
    />
    </Story>
</Preview>


## Properties

<Props of={Toggle} />
