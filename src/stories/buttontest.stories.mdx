import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean, color, text } from '@storybook/addon-knobs';
import { action } from "@storybook/addon-actions";
import { AnimatedButton } from '../components/ui/AnimatedButton/';
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

<Meta title="FusedComponents / Animated Button" component={AnimatedButton} />

# Animated Button

## Displays an image that represents a user or organization

Use an avatar for attributing actions or content to specific users. The user's name should _always_ be present when using Avatar – either printed beside the avatar or in a tooltip.

## Default

<Preview>
    <Story name="Default Animated Button" parameters={{ decorators: [withKnobs] }}>
    <AnimatedButton
        onClick={action("button action click")}
        disabled={boolean("Disabled", false)}
        primary={boolean("Primary", false)}
        isLoading={boolean("isLoading", false)}
        loadingIcon={<FontAwesomeIcon spin icon="circle-notch" />}
        completed={boolean("Completed", false)}
        buttonColor={color("Button Color", "#218280")}
    >Button</AnimatedButton>
    </Story>
</Preview>

## Default with Icon


<Preview>
    <Story name="Default Animated Button Icon" parameters={{ decorators: [withKnobs] }}>
    <AnimatedButton
        icon={<FontAwesomeIcon icon={text("Icon", "key", "DI")} />}
        onClick={action("button action click", "DI")}
        disabled={boolean("Disabled", false, "DI")}
        primary={boolean("Primary", false, "DI")}
        isLoading={boolean("isLoading", false, "DI")}
        loadingIcon={<FontAwesomeIcon spin icon="circle-notch" />}
        completed={boolean("Completed", false, "DI")}
        buttonColor={color("Button Color", "#218280", "DI")}
    >Button</AnimatedButton>
    </Story>
</Preview>

## Primary

<Preview>
<Story name="Primary Animated Button" parameters={{ decorators: [withKnobs] }}>
<AnimatedButton
    onClick={action("button action click", "Primary")}
    disabled={boolean("Disabled", false, "Primary")}
    primary={boolean("Primary", true, "Primary")}
    isLoading={boolean("isLoading", false, "Primary")}
    loadingIcon={<FontAwesomeIcon spin icon="circle-notch" />}
    completed={boolean("Completed", false, "Primary")}
    buttonColor={color("Button Color", "#218280", "Primary")}
>Button</AnimatedButton>
</Story>
</Preview>

## Primary with Icon


<Preview>
    <Story name="Primary Animated Button Icon" parameters={{ decorators: [withKnobs] }}>
    <AnimatedButton
        icon={<FontAwesomeIcon icon={text("Icon", "key", "PI")} />}
        onClick={action("button action click", "PI")}
        disabled={boolean("Disabled", false, "PI")}
        primary={boolean("Primary", true, "PI")}
        isLoading={boolean("isLoading", false, "PI")}
        loadingIcon={<FontAwesomeIcon spin icon="circle-notch" />}
        completed={boolean("Completed", false, "PI")}
        buttonColor={color("Button Color", "#218280", "PI")}
    >Button</AnimatedButton>
    </Story>
</Preview>

## Loading 

<Preview>
<Story name="Loading Animated Button" parameters={{ decorators: [withKnobs] }}>
<AnimatedButton
    onClick={action("button action click", "Loading")}
    disabled={boolean("Disabled", false, "Loading")}
    primary={boolean("Primary", false, "Loading")}
    isLoading={boolean("isLoading", true, "Loading")}
    loadingIcon={<FontAwesomeIcon spin icon="circle-notch" />}
    completed={boolean("Completed", false, "Loading")}
>Button</AnimatedButton>
</Story>
</Preview>

## Completed

<Preview>
<Story name="Completed Animated Button" parameters={{ decorators: [withKnobs] }}>
<AnimatedButton
    onClick={action("button action click", "Completed")}
    disabled={boolean("Disabled", false, "Completed")}
    primary={boolean("Primary", false, "Completed")}
    isLoading={boolean("isLoading", false, "Completed")}
    loadingIcon={<FontAwesomeIcon spin icon="circle-notch" />}
    completed={boolean("Completed", true, "Completed")}
>Loaded</AnimatedButton>
</Story>
</Preview>

## Disabled

<Preview>
<Story name="Disabled Animated Button" parameters={{ decorators: [withKnobs] }}>
<AnimatedButton
    onClick={action("button action click", "Disabled")}
    disabled={boolean("Disabled", true, "Disabled")}
    primary={boolean("Primary", false, "Disabled")}
    isLoading={boolean("isLoading", false, "Disabled")}
    loadingIcon={<FontAwesomeIcon spin icon="circle-notch" />}
    completed={boolean("Completed", false, "Disabled")}
>Button</AnimatedButton>
</Story>
</Preview>


## Properties

<Props of={AnimatedButton} />
