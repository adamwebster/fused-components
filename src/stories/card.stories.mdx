import {useState} from 'react';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean, text } from '@storybook/addon-knobs';
import { action, actions } from "@storybook/addon-actions";

import { Card } from '../components/ui/Card/';

<Meta title="FusedComponents|Card" component={Card} />

# Card

## An Card component 

## Default
<Preview>
    <Story name="Default" parameters={{ decorators: [withKnobs] }}>
    <Card padding={text('Padding', '10px')} boxShadow={boolean('Box Shadow', true)} borderRadius={text('Border Radius', '5px')}>Card</Card>
    </Story>
</Preview>

## Properties

<Props of={Card} />
