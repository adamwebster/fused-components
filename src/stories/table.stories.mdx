import { useState } from "react";
import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import {
  withKnobs,
  select,
  text,
  boolean,
} from "@storybook/addon-knobs";
import { action, actions } from "@storybook/addon-actions";
import { Table } from "../components/ui/Table/";
import { SampleData, SampleDataFixed } from '../data/SampleTableData'
import { color } from '../styles/styles';

<Meta title="FusedComponents / Table" component={Table} />

# Table

## An Table component

## Default

<Preview>
  <Story name="table">
    <Table
    data={SampleData}
    />
    </Story>
</Preview>

## Zebra Stripping

<Preview>
    <Table
      tableHeaderBGColor={color.dark}
      tableHeaderTextColor={color.light}
      data={SampleData}
      headerBorder={false}
      zebraStripping
    />
</Preview>

## Fixed

<Preview>
 <Table
    data={SampleDataFixed}
    freezeFirstColumn
    tableHeaderBGColor={color.medium}
    />
  </Preview>

## Properties

<Props of={Table} />

## Sample Data 

    const data = 
        {{
        "headers": [{ "label": "First Name", "width": "130px"} , {"label": "Last Name"}, {"label" : "Job"}],
        "rows": [
            {"row": ["Adam", "Webster", "UI Developer"]},
            {"row": ["Bruce", "Wayne", "Batman"]},
            {"row": ["Clark", "Kent", "Superman"]}
        ]
        }}
