import {useState} from 'react';
import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { action, actions } from "@storybook/addon-actions";
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';
import { DarkModeWrapper } from "../../../common/styles";
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";
import { RadioGroup } from './';
import { Radio } from '../Radio';

<Meta title="Components / Radio Group" component={RadioGroup} parameters={{ component: RadioGroup }}/>

# Radio Group

The radio group wraps multiple radios and adds functionally that is not available by default with the basic radio component such as keyboard support and only allowing one option to be selected at once. 

## Default
<Canvas>
    <Story name="Radio Group" parameters={{ decorators: []}}>
        <RadioGroup selectedValue='radio 2' onRadioChanged={(value) => console.log(value)}>
            <Radio id="1" value="radio 1">Radio 1</Radio>
            <Radio id="2" value="radio 2">Radio 2</Radio>
            <Radio id="3" value="radio 3">Radio 3</Radio>
        </RadioGroup>
    </Story>
</Canvas>

<Canvas>
    <Story name="Radio Group Inline" parameters={{ decorators: []}}>
        <RadioGroup inline onRadioChanged={(value) => console.log(value)}>
            <Radio id="1" value="radio 1">Radio 1</Radio>
            <Radio id="2" value="radio 2">Radio 2</Radio>
            <Radio id="3" value="radio 3">Radio 3</Radio>
        </RadioGroup>
    </Story>
</Canvas>

<Story name="Radio Group Dark Mode"
decorators={[storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
parameters={{ docs: { disable: true  }}}
>
    <FCThemeProvider value={{theme: 'dark'}}>
        <RadioGroup selectedValue='radio 2' onRadioChanged={(value) => console.log(value)}>
            <Radio id="1" value="radio 1">Radio 1</Radio>
            <Radio id="2" value="radio 2">Radio 2</Radio>
            <Radio id="3" value="radio 3">Radio 3</Radio>
        </RadioGroup>
    </FCThemeProvider>
</Story>


## Properties

<ArgsTable for={RadioGroup} />
