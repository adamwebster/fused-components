import {useState} from 'react';
import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { Badge } from './';
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';
import { BadgeDemoDarkMode } from "../../../demos/BadgeDemo";
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";


<Meta title="Components / Badge" component={Badge} parameters={{ component: Badge }} />

# Badge

The badge component highlight text in varying styles. 

<Canvas>
    <Story 
    argTypes={{
        'fcStyle': {control: { type: 'select', options: ['', 'danger', 'info', 'warning', 'success']}},
    }}
    name="Default">
    {(args) =>
        <Badge {...args}>Badge</Badge>
    }
    </Story>
</Canvas>



## Properties

<ArgsTable for={Badge} story="Default" />


## Dark Mode

<BadgeDemoDarkMode />
