import { useState } from 'react';
import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import variables from '../../../styles/variables';
import { AlertDemo } from '../../../demos/AlertDemo';
import { FCThemeProvider } from "../../../theming/FCTheme";
import { color } from '../../../styles/styles';

import { Alert } from './alert';
import { Icon } from '../../icon/';

<Meta title="Components / Alert" component={Alert} parameters={{ component: Alert }} />

# Alert

The alert component allows you to display an important piece of information in varying styles. 

## Default

<Canvas>
  <Story name="Default" 
  argTypes={{
    title: {control: {type: 'text'}},
    borderRadius: {control: {type: 'boolean'}},
    onMouseOver: { table: { disable: true } },
    onMouseOut: { table: { disable: true } },
    borderRadius: {control: {type: 'boolean'}},
    icon: {control: {type: 'text'}},
    fcStyle: { control: {type: 'select', options: ['', 'danger', 'info', 'warning', 'success']}}
  }}
  args={{
    title: 'title'
  }
  }
  >
    {args => 
  <Alert {...args}>Alert</Alert>
    }
  </Story>
</Canvas>


## Properties

<ArgsTable for={Alert} story="Default" />


<Canvas>
  <Story name="Danger">
  <Alert title="Danger" icon="no-entry-circle" borderRadius fcStyle="danger">
    Alert
  </Alert>
  </Story>
</Canvas>

<Canvas>
  <Story name="Success">
  <Alert
    borderRadius
    fcStyle="success"
    title="Lorem ipsum dolor sit amet, consectetur adipiscing elit. Fusce in nisl ut tortor volutpat eleifend ac eu ante. Proin sit amet purus orci. Fusce porttitor tortor sit amet diam maximus, euismod lobortis felis finibus."
    icon="check-circle"
  >
    Alert
  </Alert>
  </Story>
</Canvas>

<Canvas>
  <Story name="Warning">
  <Alert borderRadius fcStyle="warning" title="Warning" icon="exclamation-circle">
    Alert
  </Alert>
  </Story>
</Canvas>

<Canvas>
  <Story name="Info">
  <Alert borderRadius fcStyle="info" title="Info" icon="question-circle">
    Alert
  </Alert>
  </Story>
</Canvas>


<Story
  name="Default Dark mode"
  decorators={[storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
  parameters={{ docs: { disable: true }}}
>
  <FCThemeProvider value={{theme: 'dark'}}>
    <Alert title="Default">Alert</Alert>
  </FCThemeProvider>
</Story>

<Story
  name="Danger Dark mode"
  decorators={[storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
  parameters={{ docs: { disable: true }}}
>
  <FCThemeProvider value={{theme: 'dark'}}>
    <Alert title="Default" fcStyle="danger">Alert</Alert>
  </FCThemeProvider>
</Story>

<Story
  name="Waring Dark mode"
  decorators={[storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
  parameters={{ docs: { disable: true }}}
>
  <FCThemeProvider value={{theme: 'dark'}}>
    <Alert title="Default" fcStyle="warning">Alert</Alert>
  </FCThemeProvider>
</Story>


<Story
  name="Success Dark mode"
  decorators={[storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
  parameters={{ docs: { disable: true }}}
>
  <FCThemeProvider value={{theme: 'dark'}}>
    <Alert title="Default" fcStyle="success">Alert</Alert>
  </FCThemeProvider>
</Story>


<Story
  name="Info Dark mode"
  decorators={[storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
  parameters={{ docs: { disable: true }}}
>
  <FCThemeProvider value={{theme: 'dark'}}>
    <Alert title="Default" fcStyle="Info">Alert</Alert>
  </FCThemeProvider>
</Story>

## Dark Mode Canvas

<AlertDemo />