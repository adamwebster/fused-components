import {useState} from 'react';
import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { action, actions } from "@storybook/addon-actions";
import { PanelDemo, PanelDark } from '../../../demos/PanelDemo';
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";

import { Panel } from './';

<Meta title="Components / Panel" component={Panel} parameters={{ component: Panel }}/>

# Panel

The panel component is used to display information that will slide in from one of the edges of the screen.

## Sizes 
<Canvas>
    <Panel id="dialog" fixed={false} title="Dialog">
        Dialog content
    </Panel>
</Canvas>

<PanelDemo />
   

## Dark Mode

<PanelDark />


<Story name="Panel" parameters={{ docs: { disable: true  }}}>
        <Panel fixed={false}  id="panelStory" fixed={false} title="Dialog">
              Dialog content
    </Panel>
</Story>

<Story name="Panel Dark Mode"
decorators={[storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
parameters={{ docs: { disable: true  }}}
>
    <FCThemeProvider value={{theme: 'dark'}}>
        <Panel  fixed={false}  id="panelStory" fixed={false} title="Dialog">
            Dialog content
        </Panel>
    </FCThemeProvider>
</Story>

<ArgsTable for={Panel} />
