import { useState } from 'react';
import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action, actions } from '@storybook/addon-actions';
import  DatePicker from './DatePicker';
import { DatePickerDemo, DatePickerDarkDemo } from '../../../demos/DatePickerDemo';
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";


<Meta title="Components / DatePicker" component={DatePicker} parameters={{ component: DatePicker }} />

# Date Picker

The date picker component allows a date to be selected from a dropdown menu.  A date can also be directly typed into the input.
## Default

<DatePickerDemo />

## Dark mode
<DatePickerDarkDemo />

## Properties

<ArgsTable for={DatePicker} />
