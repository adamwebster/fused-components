import {useState} from 'react';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean, text } from '@storybook/addon-knobs';
import { action, actions } from "@storybook/addon-actions";
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';
import { CardDarkMode } from "../../../demos/CardDemo";
import {Input, Label, Button, FormField, Checkbox} from "../../../components";
import { Card } from './';
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";


<Meta title="Components / Card" component={Card} parameters={{ component: Card }} />

# Card

Use the card to highlight a piece of content or as a wrapper for other elements, for example a login form.

## Default


<Preview>
    <Story name="Card">
        <Card boxShadow borderRadius="5px" style={{padding: 10 + 'px'}}>
            <div style={{marginBottom: 10 + 'px'}}>
                <FormField htmlFor="username" label="Username" hint="Enter your username">
                <Input id="username" />
                </FormField>
            </div>
            <div style={{marginBottom: 10 + 'px'}}>
                <FormField htmlFor="password" label="Password" hint="Enter your password">
                <Input type="password" id="password" />
                </FormField>
            </div>
             <div style={{marginBottom: 10 + 'px'}}>
            <Checkbox>Remember my password</Checkbox>
            </div>
            <Button icon="lock-locked" primary style={{marginRight: 5 + 'px'}}>Login</Button>
            <Button>Forgot Password</Button>
        </Card>
    </Story>
</Preview>

<Story name="Card Dark Mode"
decorators={[withKnobs, storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
parameters={{ docs: { disable: true  }}}
>
    <FCThemeProvider value={{theme: 'dark'}}>
        <Card boxShadow borderRadius="5px" style={{padding: 10 + 'px'}}>
             <div style={{marginBottom: 10 + 'px'}}>
                <FormField htmlFor="username" label="Username" hint="Enter your username">
                <Input id="username" />
                </FormField>
            </div>
            <div style={{marginBottom: 10 + 'px'}}>
                <FormField validationMessage={text('Validation Message', '')} htmlFor="password" label="Password" hint="Enter your password">
                <Input type="password" id="password" />
                </FormField>
            </div>
             <div style={{marginBottom: 10 + 'px'}}>
            <Checkbox inError={boolean('Checkbox has error', false)}>Remember my password</Checkbox>
            </div>
            <Button icon="lock-locked" primary style={{marginRight: 5 + 'px'}}>Login</Button>
            <Button>Forgot Password</Button>
        </Card>
    </FCThemeProvider>
</Story>

## Dark Mode
<CardDarkMode />

## Properties

<Props of={Card} />

# Card dependents & dependencies
<DependenciesTable titleDependencies='Dependencies' titleDependents='Dependents' of={Card} />
