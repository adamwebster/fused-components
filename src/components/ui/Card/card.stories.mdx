import { useState } from 'react';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { ArgsTable } from '@storybook/addon-docs';
import { CardDarkMode } from "../../../demos/CardDemo";
import {Input, Label, Button, FormField, Checkbox} from "../../../components";
import { Card } from './';
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";


<Meta title="Components / Card" component={Card} parameters={{ component: Card }} />

# Card

Use the card to highlight a piece of content or as a wrapper for other elements, for example a login form.

## Default


<Preview>
    <Story name="Card">
        {args => (
        <Card {...args} style={{padding: 10 + 'px'}}>
            <div style={{marginBottom: 10 + 'px'}}>
                <FormField htmlFor="username" label="Username" hint="Enter your username">
                <Input id="username" />
                </FormField>
            </div>
            <div style={{marginBottom: 10 + 'px'}}>
                <FormField htmlFor="password" label="Password" hint="Enter your password">
                <Input type="password" id="password" />
                </FormField>
            </div>
             <div style={{marginBottom: 10 + 'px'}}>
            <Checkbox>Remember my password</Checkbox>
            </div>
            <Button icon="lock-locked" primary style={{marginRight: 5 + 'px'}}>Login</Button>
            <Button>Forgot Password</Button>
        </Card>
        )}
    </Story>
</Preview>

<Story name="Card Dark Mode"
decorators={[storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
parameters={{ docs: { disable: true  }}}
>{args => (
    <FCThemeProvider value={{theme: 'dark'}}>
        <Card {...args} style={{padding: 10 + 'px'}}>
             <div style={{marginBottom: 10 + 'px'}}>
                <FormField htmlFor="username" label="Username" hint="Enter your username">
                <Input id="username" />
                </FormField>
            </div>
            <div style={{marginBottom: 10 + 'px'}}>
                <FormField htmlFor="password" label="Password" hint="Enter your password">
                <Input type="password" id="password" />
                </FormField>
            </div>
             <div style={{marginBottom: 10 + 'px'}}>
            <Checkbox>Remember my password</Checkbox>
            </div>
            <Button icon="lock-locked" primary style={{marginRight: 5 + 'px'}}>Login</Button>
            <Button>Forgot Password</Button>
        </Card>
    </FCThemeProvider>
    )}
</Story>

## Dark Mode
<CardDarkMode />

## Properties

<ArgsTable for={Card} />
