import {useState} from 'react';
import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action, actions } from "@storybook/addon-actions";
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';
import { FormFieldDark, FormFieldDemo } from '../../../demos/FormFieldDemo';
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";

import { FormField } from './';
import { Textarea } from '../Textarea';

<Meta title="Components / Form Field" component={FormField} parameters={{ component: FormField }}/>

# Form Field

The form field component is a way to tie together common form items such as a label, input, descriptive message and a validation message. When creating a form that has validation it is recommended to use this component.

## Default
<Canvas>
    <Story name="Form Field" parameters={{ decorators: [withKnobs]}}>
    <FormField label="test" htmlFor="textarea" hint="This is a hint" validationMessage="Something went wrong">
    <Textarea aria-describedby="textarea_hint textarea_error" id="textarea" />
    </FormField>
    </Story>
</Canvas>

## Demo 

<FormFieldDemo />

<Story name="Form Field Dark Mode"
decorators={[withKnobs, storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
parameters={{ docs: { disable: true  }}}
>
    <FCThemeProvider value={{theme: 'dark'}}>
        <FormField label="test" htmlFor="textarea" hint="This is a hint" validationMessage="Something went wrong">
            <Textarea aria-describedby="textarea_hint textarea_error" id="textarea" />
        </FormField>
    </FCThemeProvider>
</Story>


## Dark Mode

<FormFieldDark />

## Properties

<ArgsTable for={FormField} />

# Input Field dependents & dependencies
<DependenciesTable titleDependencies='Dependencies' titleDependents='Dependents' of={FormField} />
