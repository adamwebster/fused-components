import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text, boolean } from "@storybook/addon-knobs";
import { action, actions } from "@storybook/addon-actions";
import { Checkbox } from "./";
import { CheckboxDemo, CheckboxDarkMode } from "../../../demos/CheckboxDemo";
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';

<Meta title="Components / Checkbox" component={Checkbox} parameters={{ component: Checkbox }} />

# Checkbox

The checkbox component is an replacement for the browsers checkbox element.  If the checkbox is checked or not is completely controlled by the consuming application through the **checked** property.  

<Preview>
  <Checkbox id="checkbox1">Checkbox</Checkbox>
  <Checkbox id="checkbox2" checked>Checkbox</Checkbox>
  <Checkbox id="checkbox3" inError>Checkbox</Checkbox>
  <Checkbox id="checkbox4" inWarning>Checkbox</Checkbox>
</Preview>

## Demo

<Preview>
  <CheckboxDemo />
</Preview>

## Dark Mode

<CheckboxDarkMode />

## Properties

<Props of={Checkbox} />

<Story name="Checkbox" parameters={{ decorators: [withKnobs],  docs: { disable: true  }}}>
  <Checkbox id="checkbox6" aria-label="Check me" checked={boolean('Checked')}>Check me</Checkbox>
</Story>

# Checkbox dependents & dependencies
<DependenciesTable titleDependencies='Dependencies' titleDependents='Dependents' of={Checkbox} />

