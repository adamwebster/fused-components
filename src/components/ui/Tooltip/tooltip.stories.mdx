import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action, actions } from "@storybook/addon-actions";
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';
import { Icon } from '../../icon'
import Tooltip from './tooltip';

<Meta title="Components / Tooltip" component={Tooltip} parameters={{ component: Tooltip }}/>

# Tooltip

The tooltip help information related to the thing that is being hovered over.

<Preview>
    <Tooltip content="World">
    Hello
    </Tooltip>
</Preview>

<Story name="Tooltip default settings" parameters={{ decorators: [withKnobs],  docs: { disable: true  }}}>
    <div style={{padding: 150 + 'px'}}>
    <Tooltip placement={select('Placement', ['auto', 'auto-start', 'auto-end', 'top', 'left', 'right', 'bottom', 'top-start', 'top-end', 'bottom-start', 'bottom-end', 'right-start', 'right-end', 'left-start', 'left-end'], 'auto', 'placement1')} content={"Here I am..."}>
        Where is the tooltip
    </Tooltip>
    </div>
</Story>


<Story name="Tooltip attached to the body" parameters={{ docs: { disable: true }}}>
   <>
    <div style={{position: 'relative', display: 'inline-block', border: 'solid 1px #000', padding: 30 + 'px', overflow: 'hidden'}}>
        <Tooltip targetElement="body" content={"Here I am..."}>
           I am attached to the body so I will breakout of the containing element.
        </Tooltip>
    </div>
     <div style={{position: 'relative', display: 'inline-block', border: 'solid 1px #000', padding: 30 + 'px', overflow: 'hidden'}}>
        <Tooltip content={"Here I am..."}>
           I am not so I will be hidden by the containing element.
        </Tooltip>
    </div>
    </>
</Story>

## Properties

<Props of={Tooltip} />

# Tooltip dependents & dependencies
<DependenciesTable titleDependencies='Dependencies' titleDependents='Dependents' of={Tooltip} />
