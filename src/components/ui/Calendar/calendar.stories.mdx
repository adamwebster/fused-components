import { useState } from 'react';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action, actions } from '@storybook/addon-actions';
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';
import { Calendar } from './';

<Meta title="Components / Calendar" component={Calendar} parameters={{ component: Calendar }} />

# Calendar

The calendar component can be used when you would like to show an calendar. It uses [dayjs](https://github.com/iamkun/dayjs) behind the scene to help with the date formatting.

If you use **onChange** it will pass back the formatted date in the specified format that you pass in the **dateFormat** property (defaults to MM/DD/YYYY).

## Default

<Preview>
  <Story name="Calendar">
    <Calendar onChange={(date) => console.log(date)} />
  </Story>
</Preview>


## Properties

<Props of={Calendar} />

# Label dependents & dependencies

<DependenciesTable titleDependencies="Dependencies" titleDependents="Dependents" of={Calendar} />
