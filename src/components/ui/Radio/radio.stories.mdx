import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { action, actions } from "@storybook/addon-actions";
import { Radio } from './';
import { RadioDemo, RadioDarkMode } from '../../../demos/RadioDemo';
import LinkTo from '@storybook/addon-links/react';
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";

<Meta title="Components / Radio" component={Radio} parameters={{ component: Radio }}/>

# Radio

The radio component is an replacement for the browsers radio element.  If the radio is checked or not is completely controlled by the consuming application through the **checked** property.

You can also use the <LinkTo kind="components-radio-group--radio-group">radio group</LinkTo> component to group multiple radios so only one is checked at once and add built in accessibility features.

<Canvas>
      <Radio>Radio</Radio>
       <Radio checked>Radio</Radio>
           <Radio inError>Radio</Radio>
       <Radio inWarning>Radio</Radio>
</Canvas>


## Demo 

<Canvas withSource={'none'}>
      <RadioDemo/>
</Canvas>

## Dark Mode

<RadioDarkMode />

<Story name="Radio" parameters={{ docs: { disable: true  }}}>
      <Radio >Radio</Radio>
</Story>

<Story name="Radio Dark Mode"
decorators={[storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
parameters={{ docs: { disable: true  }}}
>
    <FCThemeProvider value={{theme: 'dark'}}>
            <Radio>Radio</Radio>
    </FCThemeProvider>
</Story>

## Properties

<ArgsTable for={Radio} story="Radio" />
