import {useState} from 'react';
import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { action, actions } from "@storybook/addon-actions";
import { Icon } from '../../icon/';
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';
import { InputDarkMode } from '../../../demos/InputDemo';
 import LinkTo from '@storybook/addon-links/react';
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";

import { Input } from './';

<Meta title="Components / Input" component={Input} parameters={{ component: Input }}/>

# Input

An text input component can be set to many of the input types(for example: text, password, tel, or email).  If the input is used without a label is is recommended to use the aria-label attribute to give the input a label.

It also supports different validation styles and can have any of the <LinkTo kind="info-icons--page">icons</LinkTo> the come with **Fused Components** added to it.

## Not Active
<Canvas>
    <Story name="Input">
        <Input
            id="inputStory1"
            aria-label="Text Input" 
        />
    </Story>
</Canvas>

## Disabled 
<Canvas>
    <Input
        id="inputStory2"
        aria-label="Disabled Input" 
        disabled
    />
</Canvas>

## Error

<Canvas>
    <Input
        id="inputStory3"
        inError
        aria-label="Error Input"
    />
</Canvas>

## Warning

<Canvas>
    <Input
        id="inputStory4"
        inWarning
        aria-label="Warning Input"
    />
</Canvas>

## Icon

<Canvas>
    <Input
        icon="search"
        id="inputStory5"
        aria-label="Icon Input"
    />
    <br/>
     <Input
        inError
        icon="times-circle"
        aria-label="Icon Input"
    />
        <br/>
     <Input
        id="inputStory6"
        inWarning
        icon="exclamation-circle"
        aria-label="Icon Input"
    />
</Canvas>

<Story name="Input Dark Mode"
decorators={[storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
parameters={{ docs: { disable: true  }}}
>
    <FCThemeProvider value={{theme: 'dark'}}>
        <Input
            id="inputStory1"
            aria-label="Text Input" 
        />
    </FCThemeProvider>
</Story>

## Dark Mode 

<InputDarkMode />

## Properties

<ArgsTable for={Input} />