import { useState } from 'react';
import { Meta, Story, ArgsTable, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action, actions } from '@storybook/addon-actions';
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';
import { ButtonGroupDarkMode } from '../../../demos/ButtonGroupDemo';
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";

import { ButtonGroup } from './';
import { Button } from '../Button/';

<Meta title="Components / Button Group" component={ButtonGroup} parameters={{ component: ButtonGroup }} />

# Button Group

The button group is used to display a group of buttons with the proper styling.

## Default

<Preview>
  <ButtonGroup>
    <Button>Button 1</Button>
    <Button>Button 2</Button>
    <Button>Button 3</Button>
  </ButtonGroup>
</Preview>

## Primary Button Group

<Preview>
  <ButtonGroup>
    <Button primary>Button 1</Button>
    <Button primary>Button 2</Button>
    <Button primary>Button 3</Button>
  </ButtonGroup>
</Preview>



<Story name="Button Group" parameters={{ decorators: [withKnobs],  docs: { disable: true  }}}>
  <ButtonGroup>
    <Button  id="primaryButton" primary={boolean('Primary')} fcStyle={select('fcStyle', ['none', 'danger', 'warning', 'info', 'success'])}>Button</Button>
    <Button  id="primaryButton1" primary={boolean('Primary')} fcStyle={select('fcStyle', ['none', 'danger', 'warning', 'info', 'success'])}>Button</Button>
    <Button  id="primaryButton2" primary={boolean('Primary')} fcStyle={select('fcStyle', ['none', 'danger', 'warning', 'info', 'success'])}>Button</Button>
  </ButtonGroup>
</Story>

<Story name="Button Group Dark Mode" 
decorators={[withKnobs, storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
parameters={{ docs: { disable: true  }}}>
  <FCThemeProvider value={{theme: 'dark'}}>
    <ButtonGroup>
      <Button  id="primaryButton" primary={boolean('Primary')} fcStyle={select('fcStyle', ['none', 'danger', 'warning', 'info', 'success'])}>Button</Button>
      <Button  id="primaryButton1" primary={boolean('Primary')} fcStyle={select('fcStyle', ['none', 'danger', 'warning', 'info', 'success'])}>Button</Button>
      <Button  id="primaryButton2" primary={boolean('Primary')} fcStyle={select('fcStyle', ['none', 'danger', 'warning', 'info', 'success'])}>Button</Button>
    </ButtonGroup>
   </FCThemeProvider>
</Story>



## Dark Mode

<ButtonGroupDarkMode />

# Label dependents & dependencies

<DependenciesTable titleDependencies="Dependencies" titleDependents="Dependents" of={ButtonGroup} />
