import { useState } from 'react';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { ArgsTable } from '@storybook/addon-docs';
import { ButtonGroupDarkMode } from '../../../demos/ButtonGroupDemo';
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";
import ButtonGroup from './buttongroup';
import { Button } from '../Button/';

<Meta title="Components / Button Group" component={ButtonGroup} parameters={{ component: ButtonGroup }} />

# Button Group

The button group is used to display a group of buttons with the proper styling.

## Default

<Preview>
  <ButtonGroup>
    <Button>Button 1</Button>
    <Button>Button 2</Button>
    <Button>Button 3</Button>
  </ButtonGroup>
</Preview>

## Primary Button Group

<Preview>
  <ButtonGroup>
    <Button primary>Button 1</Button>
    <Button primary>Button 2</Button>
    <Button primary>Button 3</Button>
  </ButtonGroup>
</Preview>

<Story name="Button Group" parameters={{ docs: { disable: true  }}}>
  <ButtonGroup>
    <Button  id="primaryButton" >Button</Button>
    <Button  id="primaryButton1">Button</Button>
    <Button  id="primaryButton2">Button</Button>
  </ButtonGroup>
</Story>

<Story name="Button Group Dark Mode" 
decorators={[storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
parameters={{ docs: { disable: true  }}}>
  <FCThemeProvider value={{theme: 'dark'}}>
    <ButtonGroup>
      <Button id="primaryButton">Button</Button>
      <Button id="primaryButton1">Button</Button>
      <Button id="primaryButton2">Button</Button>
    </ButtonGroup>
   </FCThemeProvider>
</Story>

## Dark Mode

<ButtonGroupDarkMode />
