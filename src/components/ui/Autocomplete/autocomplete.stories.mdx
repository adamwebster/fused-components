import {useState} from 'react';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action, actions } from "@storybook/addon-actions";
import { Icon } from '../../icon/';
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';
import { AutocompleteDemo,AutocompleteDemoFormatter } from "../../../demos/AutocompleteDemo";
import { Autocomplete } from './';

<Meta title="Components / Auto Complete" component={Autocomplete} parameters={{ component: Autocomplete }} />

<Story name="Autocomplete" parameters={{ docs: { disable: true }}}>
	<Autocomplete
		id="ac1"
		items={['Apple', 'Banana', 'Pear']}
 		aria-label="Search for a fruit"
 	/>
</Story>

## Default
<Autocomplete
	id="ac1"
	items={['Apple', 'Banana', 'Pear']}
    inputIcon="filter"
    placeholder="Type to search for a fruit"
 />

## Item Formatter

<AutocompleteDemoFormatter />

Vie the code example below to see who this works.

### Code Example 
	const YourComponent = () => {
		const [data, setData] = useState([] as any);
		const dataToSet = [
			{ label: "Apple", description: 'Apples are red and are crunchy.' },
			{ label: "Banana", description: 'Bananas are yellow you have to peel them.' },
			{ label: "Orange", description: 'Oranges have a hard peel and are full of vitamin C.' }
		]

		return(
			<Autocomplete
				onChange={() => setData(dataToSet)}
				itemFormatter={(value) => {
					return (
					<>
						<div>{data[value].label}</div>
						<div style={{ fontSize: '12px', color: '#aaa' }}>{data[value].description}</div>
					</>
					)
				}}
				onItemClick={(index) => console.log(`I have an index of ${index}`)}
				inputIcon="search"
				placeholder="Search with formatting"
				keyToSearch="label"
				items={data}
			/>
		)
	}

	export default YourComponent;



## Dark mode

<AutocompleteDemo />

## Properties

<Props of={Autocomplete} />

# Auto Complete dependents & dependencies
<DependenciesTable titleDependencies='Dependencies' titleDependents='Dependents' of={Autocomplete} />
