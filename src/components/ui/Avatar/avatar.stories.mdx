import { useState } from 'react';
import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean, text } from '@storybook/addon-knobs';
import { action, actions } from '@storybook/addon-actions';
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';

import { Avatar } from './';

<Meta title="Components / Avatar" component={Avatar} parameters={{ component: Avatar }} />

# Avatar

The avatar component is used to show an profile image for a user.

<Canvas>
  <Story 
  argTypes={
    {
      size: {control: {type: 'select', options: ['tiny', 'small','medium', 'large']}},
      image: {control: {type: 'text'}},
      borderRadius: {control: {type: 'select', options: ['square', 'rounded','round']}},
      boxShadow: {control: 'boolean'},
    }
  }
  args={{
    image: "https://i.pravatar.cc/300"
    }}
  name="Default">{(args) =>
    <Avatar {...args} title="Avatar" />
  }
  </Story>
</Canvas>

## Properties

<ArgsTable for={Avatar} story="Default" />

# Table dependents + dependencies

<DependenciesTable titleDependencies="Dependencies" titleDependents="Dependents" of={Avatar} />
