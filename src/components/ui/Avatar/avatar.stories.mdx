import { useState } from 'react';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { ArgsTable } from '@storybook/addon-docs';

import Avatar from './avatar';

<Meta title="Components / Avatar" component={Avatar} parameters={{ component: Avatar }} args={{image: "https://i.pravatar.cc/300", size: 'large'}} />

# Avatar

The avatar component is used to show an profile image for a user.

<Preview>
  <Story name="Default">{(args) =>(
    <Avatar {...args} />
  )}
  </Story>
</Preview>

<ArgsTable for={Avatar} story="Default"  />


