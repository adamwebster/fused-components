import {useState} from 'react';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean, text } from '@storybook/addon-knobs';
import { action, actions } from "@storybook/addon-actions";
import {CornerDialog} from './';
import { CornerDialogDemo, CornerDialogDark } from '../../../demos/CornerDialogDemo';
import { Icon } from '../../icon/';
import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';
import { color } from '../../../styles/styles';
import { FCThemeProvider } from "../../../theming/FCTheme";

<Meta title="Components / Corner Dialog" component={CornerDialog}  parameters={{ component: CornerDialog }} />

# Corner Dialog

The corner dialog component allows you to show a popup in one of the corners. This could be useful to notifying the user about some information or requesting some feedback.  It requires the user to click one of the buttons to close the dialog.

## Default
<Preview>
    <CornerDialog targetElement='d1' id="default" title="Corner Dialog" fixed={false}>
        This is a Corner Dialog that will appear in the bottom right corner of the users screen. With text that should not wrap around icon.
    </CornerDialog>
</Preview>

## Styles
<Preview>
    <CornerDialog id="danger" fcStyle="danger" icon="no-entry-circle" title="Corner Dialog" fixed={false}>
        This is a Corner Dialog that will appear in the bottom right corner of the users screen. With text that should not wrap around icon.
    </CornerDialog>
        <CornerDialog id="warning" fcStyle="warning" icon="exclamation-circle" title="Corner Dialog" fixed={false}>
        This is a Corner Dialog that will appear in the bottom right corner of the users screen. With text that should not wrap around icon.
    </CornerDialog>
         <CornerDialog id="info" fcStyle="info" icon="question-circle" title="Corner Dialog" fixed={false}>
        This is a Corner Dialog that will appear in the bottom right corner of the users screen. With text that should not wrap around icon.
    </CornerDialog>
        <CornerDialog id="success" fcStyle="success" icon="check-circle" title="Corner Dialog" fixed={false}>
        This is a Corner Dialog that will appear in the bottom right corner of the users screen. With text that should not wrap around icon.
    </CornerDialog>
</Preview>

## Interactive demo

If this button is disabled close the corner dialog that is displayed.
<CornerDialogDemo />

## Dark Mode

<CornerDialogDark />


<Story name="Corner Dialog" parameters={{ decorators: [withKnobs],  docs: { disable: true  }}}>
    <CornerDialog targetElement='d1' id="default" title="Corner Dialog" fcStyle={select('fcStyle', ['none', 'danger', 'warning', 'info', 'success'])} fixed={false}>
    This is a Corner Dialog that will appear in the bottom right corner of the users screen. With text that should not wrap around icon.
    </CornerDialog>
</Story>


<Story name="Corner Dialog Dark Mode"
decorators={[withKnobs, storyFn => <div style={{ backgroundColor: color.darkModeBG, height: 'calc(' + 100 + 'vh - ' + 20 + 'px)' , boxSizing: 'border-box', padding: 40 + 'px' }}>{storyFn()}</div>]}
parameters={{ docs: { disable: true  }}}
>
    <FCThemeProvider value={{theme: 'dark'}}>
        <CornerDialog targetElement='d1' id="default" title="Corner Dialog" fcStyle={select('fcStyle', ['none', 'danger', 'warning', 'info', 'success'])} fixed={false}>
        This is a Corner Dialog that will appear in the bottom right corner of the users screen. With text that should not wrap around icon.
        </CornerDialog>
    </FCThemeProvider>
</Story>

## Properties

<Props of={CornerDialog} />

# Corner Dialog dependents & dependencies
<DependenciesTable titleDependencies='Dependencies' titleDependents='Dependents' of={CornerDialog} />
